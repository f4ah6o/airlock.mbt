# airlock

## Run it-support-app (native)

```bash
cd src
moon run cmd/it_support_app --target native
```

The server listens on `http://0.0.0.0:8080`.

## Update vendored mhx runtime

```bash
./src/cmd/it_support_app/scripts/build_mhx.sh
```
